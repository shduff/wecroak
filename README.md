# WeCroak static site

Public files for a tiny WeCroak landing page, privacy policy, and terms of service.

Before publishing, replace `hello@example.com` in `privacy.html` and `terms.html` with your contact email.

To publish with GitHub Pages, put these files in a repository and enable Pages for the repo's main branch.
